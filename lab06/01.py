from math import prod

# Пример списка чисел
numbers = [1, 2, 3, 4, 5]

# Используем встроенную функцию prod() для перемножения всех элементов списка
result = prod(numbers)

print("Произведение всех чисел:", result)
